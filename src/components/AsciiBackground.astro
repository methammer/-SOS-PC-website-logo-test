---
// No server-side logic needed for the component itself
---
<canvas id="ascii-background-canvas"></canvas>

<style>
  #ascii-background-canvas {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    opacity: 0.15; /* Suggestion: Temporarily increase to 0.5 or 1.0 for debugging the effect */
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const canvas = document.getElementById('ascii-background-canvas') as HTMLCanvasElement;
    if (!canvas) {
      console.error("AsciiBackground: Canvas element not found.");
      return;
    }
    const ctx = canvas.getContext('2d');
    if (!ctx) {
      console.error("AsciiBackground: Could not get 2D context.");
      return;
    }

    // --- Configuration ---
    const charSet = ['.', ':', '-', '|', '*', '+', '~'];
    const baseFontSize = 16;
    const updateInterval = 200;
    const changeProbability = 0.02;

    const lightThemeAsciiColors = ['#6c757d', '#5a6268', '#495057', '#343a40', '#212529'];
    // Adjusted for better contrast with brightening effect in dark mode
    const darkThemeAsciiColors = ['#707070', '#888888', '#909090', '#A0A0A0', '#B0B0B0']; 
    
    const mouseEffectRadius = 150; // Pixels
    const maxBrightnessIncrease = 0.9; // Increased slightly for more impact

    let currentAsciiColors: string[] = [];
    let dpr = window.devicePixelRatio || 1;

    let cols: number;
    let rows: number;
    let grid: { char: string; color: string }[][] = [];
    
    let lastCharUpdateTime = 0;
    let mouseX = -Infinity; 
    let mouseY = -Infinity;
    let hasLoggedEffect = false; // To prevent console flooding

    // --- Helper Functions for Color Manipulation ---
    function hexToRgb(hex: string): { r: number; g: number; b: number } | null {
      const result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
      return result ? {
        r: parseInt(result[1], 16),
        g: parseInt(result[2], 16),
        b: parseInt(result[3], 16)
      } : null;
    }

    function rgbToHex(r: number, g: number, b: number): string {
      return "#" + ((1 << 24) + (r << 16) + (g << 8) + b).toString(16).slice(1).toUpperCase();
    }

    function brightenColor(hexColor: string, factor: number): string {
      const rgb = hexToRgb(hexColor);
      if (!rgb) return hexColor;
      const clampedFactor = Math.max(0, Math.min(1, factor));
      const r = Math.min(255, Math.round(rgb.r + (255 - rgb.r) * clampedFactor));
      const g = Math.min(255, Math.round(rgb.g + (255 - rgb.g) * clampedFactor));
      const b = Math.min(255, Math.round(rgb.b + (255 - rgb.b) * clampedFactor));
      return rgbToHex(r, g, b);
    }

    // --- Core Logic ---
    function updateThemeColors() {
      const isDarkMode = document.documentElement.classList.contains('dark');
      // console.log("AsciiBackground: Updating theme. Dark mode:", isDarkMode);
      if (isDarkMode) {
        currentAsciiColors = darkThemeAsciiColors;
      } else {
        currentAsciiColors = lightThemeAsciiColors;
      }
    }

    function initializeGridData() {
      if (currentAsciiColors.length === 0) {
        // console.warn("AsciiBackground: currentAsciiColors is empty during grid initialization. Forcing theme update.");
        updateThemeColors(); // Ensure colors are loaded
      }
      if (currentAsciiColors.length === 0) {
        // console.error("AsciiBackground: currentAsciiColors is still empty. Cannot initialize grid.");
        return; // Prevent errors if colors still not loaded
      }

      cols = Math.ceil(window.innerWidth / baseFontSize);
      rows = Math.ceil(window.innerHeight / baseFontSize);

      grid = [];
      for (let y = 0; y < rows; y++) {
        grid[y] = [];
        for (let x = 0; x < cols; x++) {
          grid[y][x] = {
            char: charSet[Math.floor(Math.random() * charSet.length)],
            color: currentAsciiColors[Math.floor(Math.random() * currentAsciiColors.length)],
          };
        }
      }
      // console.log(`AsciiBackground: Grid initialized (${cols}x${rows})`);
    }

    function setupCanvas() {
      updateThemeColors();
      dpr = window.devicePixelRatio || 1;

      canvas.width = window.innerWidth * dpr;
      canvas.height = window.innerHeight * dpr;
      ctx.scale(dpr, dpr);

      ctx.font = `${baseFontSize}px monospace`;
      ctx.textAlign = 'left';
      ctx.textBaseline = 'top';
      
      initializeGridData();
    }

    function draw() {
      ctx.clearRect(0, 0, window.innerWidth, window.innerHeight);
      if (!grid || grid.length === 0) return; // Don't draw if grid not ready

      let effectAppliedThisFrame = false;

      for (let y = 0; y < rows; y++) {
        for (let x = 0; x < cols; x++) {
          if (!grid[y] || !grid[y][x]) continue; // Cell might not exist if resizing
          const cell = grid[y][x];
          
          const charX = x * baseFontSize + baseFontSize / 2;
          const charY = y * baseFontSize + baseFontSize / 2;
          
          const distanceToMouse = Math.sqrt(Math.pow(charX - mouseX, 2) + Math.pow(charY - mouseY, 2));
          let displayColor = cell.color;

          if (distanceToMouse < mouseEffectRadius) {
            const intensity = 1 - (distanceToMouse / mouseEffectRadius);
            const brightnessFactor = intensity * maxBrightnessIncrease;
            const originalColor = cell.color;
            displayColor = brightenColor(originalColor, brightnessFactor);
            effectAppliedThisFrame = true;

            if (!hasLoggedEffect && displayColor !== originalColor) {
              // console.log(`AsciiBackground: Effect triggered! Pos:(${charX.toFixed(0)},${charY.toFixed(0)}), Dist: ${distanceToMouse.toFixed(0)}, Factor: ${brightnessFactor.toFixed(2)}, Original: ${originalColor}, New: ${displayColor}`);
              hasLoggedEffect = true; // Log once per interaction burst
              setTimeout(() => { hasLoggedEffect = false; }, 500); // Reset log flag
            }
          }
          
          ctx.fillStyle = displayColor;
          ctx.fillText(cell.char, x * baseFontSize, y * baseFontSize);
        }
      }
      // if (effectAppliedThisFrame) console.log("AsciiBackground: Mouse effect applied in this frame.");
    }

    function updateCharacterGridContent() {
       if (!grid || grid.length === 0) return;
       for (let y = 0; y < rows; y++) {
         for (let x = 0; x < cols; x++) {
           if (grid[y] && grid[y][x] && Math.random() < changeProbability) { 
              grid[y][x].char = charSet[Math.floor(Math.random() * charSet.length)];
              if (currentAsciiColors.length > 0) {
                grid[y][x].color = currentAsciiColors[Math.floor(Math.random() * currentAsciiColors.length)];
              }
           }
         }
       }
    }

    function animate(timestamp: number) {
      const deltaTime = timestamp - lastCharUpdateTime;
      if (deltaTime > updateInterval) {
        updateCharacterGridContent();
        lastCharUpdateTime = timestamp;
      }

      draw(); 
      requestAnimationFrame(animate);
    }

    // --- Event Listeners ---
    window.addEventListener('mousemove', (event) => {
      mouseX = event.clientX;
      mouseY = event.clientY;
      // console.log(`AsciiBackground: Mouse move: ${mouseX}, ${mouseY}`); // Uncomment for verbose mouse tracking
    });
    
    window.addEventListener('mouseout', () => {
      mouseX = -Infinity;
      mouseY = -Infinity;
    });

    // --- Initialization ---
    setupCanvas(); // Initial setup

    let resizeTimeout: number;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = window.setTimeout(() => {
        // console.log("AsciiBackground: Resizing canvas...");
        setupCanvas();
      }, 250);
    });

    const themeObserver = new MutationObserver((mutationsList) => {
      for (const mutation of mutationsList) {
        if (mutation.type === 'attributes' && mutation.attributeName === 'class') {
          // console.log("AsciiBackground: Theme changed via class attribute.");
          const previousColorsString = JSON.stringify(currentAsciiColors);
          updateThemeColors();
          if (JSON.stringify(currentAsciiColors) !== previousColorsString) {
            // console.log("AsciiBackground: ASCII colors changed, re-initializing canvas & grid.");
            initializeGridData(); // Re-initialize grid with new colors, but don't rescale canvas unless size changed
          }
          break;
        }
      }
    });

    themeObserver.observe(document.documentElement, { attributes: true });

    requestAnimationFrame(animate);
    // console.log("AsciiBackground: Script initialized and animation started.");
  });
</script>
